# AI Employee — Python dependencies
# Cloud VM: pip3 install -r requirements.txt
# Local:    uv sync  (uses pyproject.toml)

# Silver Tier — Gmail + Playwright + file watching
google-api-python-client>=2.192.0
google-auth>=2.49.1
google-auth-oauthlib>=1.3.0
playwright>=1.58.0
watchdog>=6.0.0

# Gold Tier — Odoo MCP server
mcp>=1.0.0
